# Variables de Entorno - MB Solutions Backend
# INSTRUCCIONES:
# 1. Copia este archivo como .env
# 2. Reemplaza los valores con los reales
# 3. NUNCA commits .env al repositorio (revisa .gitignore)

# Puerto del servidor
PORT=3000

# Base de datos
DB_PATH=./data/productos.json

# Credenciales Admin (CAMBIA ESTOS EN PRODUCCIÓN)
ADMIN_USER=tu_usuario_admin
ADMIN_PASSWORD=tu_contraseña_fuerte_aqui

# JWT Secret - DEBE SER UNA CLAVE SEGURA Y ÚNICA EN PRODUCCIÓN
# Genera una con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=GENERA_UNA_CLAVE_SEGURA_EN_PRODUCCION

# Node Environment
NODE_ENV=development

# Configuración SMTP para envío de emails (opcional)
# Descomenta y configura con los datos de tu servidor SMTP
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=tu_email@gmail.com
# SMTP_PASS=tu_contraseña_app
# SMTP_FROM=tu_email@gmail.com

# Para BanaHosting/cPanel, usa el servidor SMTP local:
SMTP_HOST=localhost
SMTP_PORT=25
SMTP_USER=ordenes-web@mbsolutionscr.com
SMTP_PASS=contraseña_de_ordenes_web
SMTP_FROM=ordenes-web@mbsolutionscr.com
